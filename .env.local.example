# Variáveis de ambiente para desenvolvimento local
# Copie para .env.local e ajuste conforme necessário

# URL base do backend (apontar para o serviço backend em execução)
NEXT_PUBLIC_BACKEND_URL=http://localhost:4000

# Injetar tenant em desenvolvimento para testes locais
# Defina como 'true' somente em dev/local
NEXT_PUBLIC_INJECT_TENANT=true

# Tenant padrão a ser injetado em dev
NEXT_PUBLIC_DEFAULT_TENANT=default

# Ambiente
NODE_ENV=development
